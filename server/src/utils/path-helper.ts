import path from 'path';

export const srcPath = path.resolve(path.join(__dirname, '../'));
export const serverPath = path.resolve(path.join(srcPath, '../'));
export const rootPath = path.resolve(path.join(serverPath, '../'));
